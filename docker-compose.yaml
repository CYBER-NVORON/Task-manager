services:
  web:
    build: .
    container_name: task-manager-web
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - .:/app
      - sqlite_data:/app/instance
    restart: unless-stopped

volumes:
  sqlite_data:
